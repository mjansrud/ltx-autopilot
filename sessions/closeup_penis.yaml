# ─────────────────────────────────────────────────────────────
#  Session: close-up penis
#  Inherits everything from ../config.yaml (same LoRA, same workspace,
#  same state + download archive — training continues where it left off).
#  Only the fields below override the parent.
#
#  Activate:
#      python main.py --config sessions/closeup_penis.yaml
# ─────────────────────────────────────────────────────────────

extends: ../config.yaml

crawler:
  focus: |
    Adult videos where an erect penis (cock, dick) is the main subject of
    the frame. The penis should be clearly visible and prominent throughout.
    Main actions to rotate across batches: blowjobs, deepthroats, handjobs,
    titfucks, cumshots, facials. Prefer POV perspective and wet, glossy,
    high-quality footage.

  # Drop custom_urls for this session — we want fresh LLM queries only
  custom_urls: []
